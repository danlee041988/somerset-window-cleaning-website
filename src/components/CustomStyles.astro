---
// Import Inter font with fallback handling
try {
  await import('@fontsource-variable/inter');
} catch (error) {
  console.warn('Font loading failed, using system fonts as fallback');
}

// 'DM Sans'
// Nunito
// Dosis
// Outfit
// Roboto
// Literata
// 'IBM Plex Sans'
// Karla
// Poppins
// 'Fira Sans'
// 'Libre Franklin'
// Inconsolata
// Raleway
// Oswald
// 'Space Grotesk'
// Urbanist
---

<style is:inline>
  :root {
    --aw-font-sans: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    --aw-font-serif: 'Inter Variable', Georgia, serif;
    --aw-font-heading: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;

    /* Brand: red, black, white - Company colors */
    --aw-color-primary: rgb(255 0 0); /* #FF0000 - Bright red brand color */
    --aw-color-secondary: rgb(0 0 0);   /* #000000 - Pure black */
    --aw-color-accent: rgb(149 1 1);  /* #950101 - Medium red for accents */

    --aw-color-text-heading: rgb(0 0 0);
    --aw-color-text-default: rgb(16 16 16);
    --aw-color-text-muted: rgb(31 41 55); /* Gray-800 for much better contrast */
    --aw-color-bg-page: rgb(255 255 255);

    /* Pure black for clean professional look */
    --aw-color-bg-page-dark: rgb(0 0 0);

    ::selection {
      background-color: lavender;
    }
  }

  .dark {
    --aw-font-sans: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    --aw-font-serif: 'Inter Variable', Georgia, serif;
    --aw-font-heading: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;

    --aw-color-primary: rgb(255 0 0); /* #FF0000 - Bright red brand color */
    --aw-color-secondary: rgb(255 255 255); /* White for dark mode secondary */
    --aw-color-accent: rgb(149 1 1); /* #950101 - Medium red for accents */

    --aw-color-text-heading: rgb(255 255 255);
    --aw-color-text-default: rgb(229 229 229);
    --aw-color-text-muted: rgb(209 213 219); /* Gray-300 for better dark mode contrast */
    --aw-color-bg-page: rgb(0 0 0);

    ::selection {
      background-color: black;
      color: snow;
    }
  }
</style>
